(projekty/ziolowy/streszczenie_projektu.md is a markdown file, which is a source/config file, so according to rule 6, since there are only source/config files and no build artifacts, dependencies, or temp files, the output should be empty)

```markdown

```